name: Nightly Build

on:
  repository_dispatch:
    types: [nightly]
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to build (commit SHA or branch, defaults to master)"
        required: false
        default: "master"
        type: string

concurrency:
  group: nightly-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prepare:
    name: Prepare Nightly Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      release_id: ${{ steps.release.outputs.id }}
      nightly_version: ${{ steps.version.outputs.nightly_version }}
      ref: ${{ steps.params.outputs.ref }}
      release_body: ${{ steps.changelog.outputs.notes }}
    steps:
      - name: Get build parameters
        id: params
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            echo "ref=${{ github.event.client_payload.ref }}" >> $GITHUB_OUTPUT
          else
            echo "ref=${{ github.event.inputs.ref }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout private source code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: Voice-Wise/VoiceWise
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}
          ref: ${{ steps.params.outputs.ref }}
          fetch-depth: 0
          fetch-tags: true

      - name: Compute nightly version
        id: version
        shell: bash
        run: |
          set -euo pipefail

          BASE_VERSION="$(python - <<'PY'
          import json
          from pathlib import Path
          v = json.loads(Path("package.json").read_text(encoding="utf-8")).get("version","").strip()
          print(v, end='')
          PY
          )"

          if [[ ! "${BASE_VERSION}" =~ ^[0-9]+[.][0-9]+[.][0-9]+$ ]]; then
            echo "Invalid base version in package.json: ${BASE_VERSION}" >&2
            exit 1
          fi

          MAJOR="${BASE_VERSION%%.*}"
          REST="${BASE_VERSION#*.}"
          MINOR="${REST%%.*}"
          PATCH="${REST#*.}"

          NEXT_PATCH="$((PATCH + 1))"

          # Use fixed nightly version without date/SHA to ensure file overwrites
          NIGHTLY_VERSION="${MAJOR}.${MINOR}.${NEXT_PATCH}-nightly"
          echo "nightly_version=${NIGHTLY_VERSION}" >> "$GITHUB_OUTPUT"
          echo "Nightly version: ${NIGHTLY_VERSION}"

      - name: Generate nightly changelog from commits
        id: changelog
        shell: bash
        env:
          DASHSCOPE_API_KEY: ${{ secrets.DASHSCOPE_API_KEY }}
        run: |
          set -euo pipefail

          CURRENT_REF="${{ steps.params.outputs.ref }}"
          LAST_STABLE_TAG="$(git describe --tags --abbrev=0 --match 'v*' 2>/dev/null || true)"

          if [[ -n "${LAST_STABLE_TAG}" ]]; then
            RANGE="${LAST_STABLE_TAG}..HEAD"
            COMPARE_URL="https://github.com/Voice-Wise/VoiceWise/compare/${LAST_STABLE_TAG}...${CURRENT_REF}"
          else
            RANGE="HEAD"
            COMPARE_URL=""
          fi

          COMMITS="$(git log --reverse --no-merges --format='%s' ${RANGE} | grep -v '^chore(release):' || true)"

          HEADER="## Nightly Build\n\n- Version: v${{ steps.version.outputs.nightly_version }}\n- Ref: ${CURRENT_REF}\n"
          if [[ -n "${COMPARE_URL}" ]]; then
            HEADER="${HEADER}- Compare: ${COMPARE_URL}\n"
          fi

          if [[ -z "$COMMITS" ]]; then
            NOTES="${HEADER}\n\n## Changelog\n\n- No changes.\n"
          else
            PROMPT="You are generating release notes for a NIGHTLY build. Based on the following git commit messages, generate a concise and user-friendly changelog in BOTH Chinese (Simplified) and English. First output the Chinese version, then add a horizontal rule (---), then output the English version. Group changes by category (e.g., ✨ 新功能/New Features, 🐛 问题修复/Bug Fixes, ⚡ 性能优化/Performance, 🔧 其他改进/Other Improvements). Only include meaningful changes, skip trivial commits. Use clear, non-technical language. Output ONLY the changelog content in markdown format without any preamble.\n\nCommit messages:\n$COMMITS"

            RESPONSE=$(curl -s --max-time 30 "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions" \
              -H "Authorization: Bearer $DASHSCOPE_API_KEY" \
              -H "Content-Type: application/json" \
              -d "$(jq -n --arg prompt "$PROMPT" '{
                "model": "qwen-plus-latest",
                "messages": [{"role": "user", "content": $prompt}],
                "max_tokens": 1024
              }')" 2>/dev/null || echo "{}")

            CHANGELOG=$(echo "$RESPONSE" | jq -r '.choices[0].message.content // empty' 2>/dev/null || true)

            if [[ -z "$CHANGELOG" ]]; then
              NOTES="${HEADER}\n\n## Changelog\n\nSee source repository for details.\n"
            else
              NOTES="${HEADER}\n\n## Changelog\n\n${CHANGELOG}\n"
            fi
          fi

          echo "notes<<EOF" >> "$GITHUB_OUTPUT"
          echo -e "$NOTES" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Delete existing nightly release and tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Delete existing release if exists
          gh release delete nightly --yes --cleanup-tag 2>/dev/null || true

      - name: Create GitHub prerelease (nightly)
        id: release
        uses: softprops/action-gh-release@da05d552573ad5aba039eaac05058a918a7bf631 # v2.2.2
        with:
          tag_name: nightly
          name: VoiceWise Nightly (${{ steps.version.outputs.nightly_version }})
          body: ${{ steps.changelog.outputs.notes }}
          generate_release_notes: false
          draft: false
          prerelease: true

  package:
    name: Package (${{ matrix.name }})
    needs: [prepare]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS (aarch64)
            os: macos-latest
            target: aarch64-apple-darwin
            os_target: darwin
            arch: aarch64
            tauri_args: --target aarch64-apple-darwin
            artifact_name: voicewise-nightly-macos-aarch64-apple-darwin
            artifact_paths: |
              src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
              src-tauri/target/aarch64-apple-darwin/release/bundle/macos/*.app.tar.gz*

          - name: macOS (x86_64)
            os: macos-latest
            target: x86_64-apple-darwin
            os_target: darwin
            arch: x86_64
            tauri_args: --target x86_64-apple-darwin
            artifact_name: voicewise-nightly-macos-x86_64-apple-darwin
            artifact_paths: |
              src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg
              src-tauri/target/x86_64-apple-darwin/release/bundle/macos/*.app.tar.gz*

          # Windows build temporarily disabled due to slow CI times
          # - name: Windows (x86_64)
          #   os: windows-latest
          #   target: x86_64-pc-windows-msvc
          #   os_target: windows
          #   arch: x86_64
          #   tauri_args: --target x86_64-pc-windows-msvc --bundles nsis
          #   artifact_name: voicewise-nightly-windows-x86_64-pc-windows-msvc
          #   artifact_paths: |
          #     src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe*

    steps:
      - name: Checkout private source code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: Voice-Wise/VoiceWise
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Enable git long paths (Windows)
        if: ${{ runner.os == 'Windows' }}
        run: git config --system core.longpaths true

      - name: Setup Bun
        uses: oven-sh/setup-bun@8f24390df009a496891208e5e36b8a1de1f45135 # v2.0.2
        with:
          bun-version: "1.3.2"

      - name: Cache Bun
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-${{ matrix.target }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.target }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Setup Rust (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo
        uses: Swatinem/rust-cache@9d47c6ad4b02e050fd481d890b2ea34778fd09d6 # v2.7.8
        with:
          workspaces: |
            src-tauri -> target

      - name: Run Rust tests
        run: bun run test:rust

      - name: Cache cargo-edit
        id: cache-cargo-edit
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: ~/.cargo/bin/cargo-set-version*
          key: cargo-edit-${{ runner.os }}-${{ runner.arch }}

      - name: Install cargo-edit
        if: steps.cache-cargo-edit.outputs.cache-hit != 'true'
        run: cargo install cargo-edit --locked

      - name: Patch version for nightly build
        env:
          NIGHTLY_VERSION: ${{ needs.prepare.outputs.nightly_version }}
        run: |
          set -euo pipefail

          # Patch package.json and tauri.conf.json
          python - <<'PY'
          import json, os
          from pathlib import Path

          version = os.environ["NIGHTLY_VERSION"].strip()
          if not version:
              raise SystemExit("Missing NIGHTLY_VERSION")

          for path in ["package.json", "src-tauri/tauri.conf.json"]:
              p = Path(path)
              data = json.loads(p.read_text(encoding="utf-8"))
              data["version"] = version
              p.write_text(json.dumps(data, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
          PY

          # Patch Cargo.toml
          cargo set-version --manifest-path src-tauri/Cargo.toml "$NIGHTLY_VERSION"

      - name: Install NSIS (Windows)
        if: ${{ runner.os == 'Windows' }}
        run: |
          choco install nsis -y --no-progress
          if (Test-Path 'C:\Program Files (x86)\NSIS') {
            'C:\Program Files (x86)\NSIS' | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          } elseif (Test-Path 'C:\Program Files\NSIS') {
            'C:\Program Files\NSIS' | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }

      - name: Import Apple code signing cert (early)
        if: ${{ runner.os == 'macOS' }}
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail

          KEYCHAIN_PASSWORD="$(openssl rand -base64 32)"
          KEYCHAIN_PATH="$RUNNER_TEMP/voicewise-build.keychain-db"
          CERT_PATH="$RUNNER_TEMP/apple_cert.p12"

          export CERT_PATH
          python3 - <<'PY'
          import base64, os, pathlib

          b64 = os.environ["APPLE_CERTIFICATE"]
          b64 = "".join(b64.split())
          data = base64.b64decode(b64)
          pathlib.Path(os.environ["CERT_PATH"]).write_bytes(data)
          PY

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$CERT_PATH" -k "$KEYCHAIN_PATH" -P "$APPLE_CERTIFICATE_PASSWORD" -A -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH"
          security default-keychain -s "$KEYCHAIN_PATH"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

      - name: Build + upload assets (macOS)
        if: ${{ runner.os == 'macOS' }}
        uses: tauri-apps/tauri-action@v0.6.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          tauriScript: "bun run tauri"
          tagName: nightly
          releaseId: ${{ needs.prepare.outputs.release_id }}
          releaseBody: ""
          args: ${{ matrix.tauri_args }}
          assetNamePattern: "[name]_[version]_[platform]_[arch][ext]"

      - name: Build + upload assets (Windows)
        if: ${{ runner.os == 'Windows' }}
        uses: tauri-apps/tauri-action@v0.6.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tauriScript: "bun run tauri"
          tagName: nightly
          releaseId: ${{ needs.prepare.outputs.release_id }}
          releaseBody: ""
          args: ${{ matrix.tauri_args }}
          assetNamePattern: "[name]_[version]_[platform]_[arch][ext]"

      - name: Upload artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_paths }}
          if-no-files-found: error

      - name: Checkout release repo
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: main
          fetch-depth: 0
          path: release-repo

      - name: Generate manifest for ${{ matrix.os_target }}-${{ matrix.arch }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          python release-repo/scripts/generate-updater-manifests.py \
            --owner "Voice-Wise" \
            --repo "release" \
            --tag "nightly" \
            --version "${{ needs.prepare.outputs.nightly_version }}" \
            --out-dir "release-repo" \
            --channel nightly \
            --format scoped-target \
            --platforms "${{ matrix.os_target }}-${{ matrix.arch }}"

      - name: Upload manifest to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        working-directory: release-repo
        run: |
          gh release upload "nightly" \
            latest-*-nightly-*.json \
            --clobber

      - name: Commit + push manifest
        shell: bash
        working-directory: release-repo
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add latest-*-nightly-*.json
          if git diff --cached --quiet; then
            echo "No manifest changes."
            exit 0
          fi

          git commit -m "chore(nightly): update manifest for ${{ matrix.os_target }}-${{ matrix.arch }} (${{ needs.prepare.outputs.nightly_version }})"

          for attempt in 1 2 3; do
            if git push origin HEAD:main; then
              echo "Push succeeded on attempt $attempt."
              exit 0
            fi
            echo "Push failed on attempt $attempt, rebasing and retrying..."
            git pull --rebase origin main
            sleep $((attempt * 2))
          done

          echo "Push failed after 3 attempts."
          exit 1
